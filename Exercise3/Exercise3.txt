Observation: MPI divided the 10,000,000 random points among the processes and combined their counts to estimate Pi. The result was close to 3.14159 but not exact because Monte Carlo sampling is random. The runtime can change with the number of processes.